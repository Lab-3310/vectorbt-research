# This is an exampel for user to use in notebook 



# import sys, os
# sys.path.insert(1, '/Users/chouwilliam/vectorbt-research')

# from strategies.dmi_strategy import DMIStrategy

# strategy_class = 'DMIStrategy'
# strategy_config = 'dmi_config_01'

# strategy = DMIStrategy(strategy_class, strategy_config)
# strategy.run_backtest()